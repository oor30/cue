# Cueのプライバシー方針

Cueはローカルファーストで動作し、会議データを利用者のMac内に保存します。

## 現在保存するデータ

- Project Rootと検索ポリシー
- 会議のタイトル、開始・終了日時、状態
- 記録を一時停止・再開した時刻
- 文字起こし
- Meeting State、検出イベント、助言カードと根拠
- Meeting Review、診断レポート
- AIが生成した会議要約と、その根拠となる発言ID
- 利用者が承認前に確認する資料差分案とBacklog下書き

保存先は`~/Library/Application Support/Cue/`です。

## 現在保存しないデータ

- 生音声
- 画面画像
- 録画映像

画面フレームは端末内OCR後に破棄します。Backlog APIキーはSQLiteへ保存せず、Keychainへ保存します。

音声ソースとマイク端末の選択は、このMacのアプリ設定として保存します。選択したマイクが切断されている場合はPCのデフォルト入力へ戻します。

## AI Provider

CueはCodexまたはClaude Codeを読取り専用設定で利用します。選択したProject Root、明示的に許可した追加参照先、会議の文字起こし、抽出した画面文字列が分析対象になり得ます。

Web検索は初期状態で無効です。有効化した場合だけ、会議内容から作った検索語が外部サービスへ送信されます。

## 今後の音声・声紋機能

生音声保存、画面根拠画像、話者分離、声紋照合は明示設定として追加予定です。声紋はローカルで暗号化し、登録・削除・再登録と信頼度確認を利用者が制御できる設計にします。
