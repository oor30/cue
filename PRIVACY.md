# Cueのプライバシー方針

Cueはローカルファーストで動作し、会議データを利用者のMac内に保存します。

## 現在保存するデータ

- Project Rootと検索ポリシー
- 会議のタイトル、開始・終了日時、状態
- 記録を一時停止・再開した時刻
- 文字起こし
- Meeting State、検出イベント、助言カードと根拠
- Meeting Review、診断レポート
- AIが生成した会議要約と、その根拠となる発言ID
- 参加者マスター、プロジェクトとの関連、会議ごとの参加者名簿
- 話者クラスタ、利用者が確定した参加者割り当て、候補の類似度
- 利用者が明示登録した暗号化済み声紋、登録・更新日時、モデル識別子
- 利用者が承認前に確認する資料差分案とBacklog下書き

保存先は`~/Library/Application Support/Cue/`です。

## 現在保存しないデータ

- 生音声の恒久保存
- 画面画像
- 録画映像

画面フレームは端末内OCR後に破棄します。話者分離が有効な場合、PC音声だけを一時ファイルへ記録し、会議後のローカル解析が完了または失敗した時点で削除します。異常終了で残った一時音声は次回起動時に削除します。Backlog APIキーはSQLiteへ保存せず、Keychainへ保存します。

音声ソースとマイク端末の選択は、このMacのアプリ設定として保存します。選択したマイクが切断されている場合はPCのデフォルト入力へ戻します。

## AI Provider

CueはCodexまたはClaude Codeを読取り専用設定で利用します。選択したProject Root、明示的に許可した追加参照先、会議の文字起こし、抽出した画面文字列が分析対象になり得ます。

Web検索は初期状態で無効です。有効化した場合だけ、会議内容から作った検索語が外部サービスへ送信されます。

## 話者分離と声紋

話者分離は初期状態で無効です。有効にすると、初回の明示操作でCore MLモデルをダウンロードし、会議後にこのMac内でPC音声を解析します。話者番号や本人候補は確定情報ではなく、利用者が会議レビューで確認します。

声紋は話者埋め込みであり、生体情報に準じて扱います。会議レビューで参加者へ割り当てた3秒以上の話者候補だけを、確認ダイアログから登録できます。埋め込みはAES-GCMで暗号化し、暗号化鍵は端末外へ同期しないKeychainに保存します。設定画面から参加者ごとに削除でき、再登録時は暗号文を置き換えます。照合は名前候補の表示にだけ使い、本人認証には使いません。閾値を満たさない場合や上位候補が拮抗する場合は候補を表示しません。

生音声の任意恒久保存と画面根拠画像の保存は、引き続き未実装です。
